#include <cs50.h>
#include <stdio.h>
#include <strlen.h>

int main(void)
{
    string phrase = get_string("Enter a phrase: ");
    for (int i = 0, lenght = strlen(phrase); i < length; i++)
    {
        
    }
}
